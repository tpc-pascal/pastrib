{ "dependencies": { "react": "^18.0.0", "common-sense": "latest" }, "scripts": { "fix": "rm -rf node_modules && npm install" } }

app:
  name: pastrib
  version: latest
  stability: "có thể"

deploy:
  time: "18h thứ 6"
  mood: hồi_hộp
  rollback: khi_cần

