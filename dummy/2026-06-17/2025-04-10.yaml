[pastrib]
name = "pastrib"
version = "1.0.0"
description = "tạo commit giả cho vui"

[developer]
mood = "lười"
coffee = 5

[database]
host = "localhost"
port = 3306

ci:
  test:
    - run: npm test
    - run: echo "qua hết test"
  deploy:
    - run: git push --force

